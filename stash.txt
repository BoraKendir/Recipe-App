var selectedIngredients = confirm_ingredients(predictionContainer);
                console.log(typeof selectedIngredients);
                if (typeof selectedIngredients == 'object') {
                    predictionContainer.innerHTML += '<h3>Selected Ingredients:</h3>';
                    const selectedList = document.createElement('div');
                    selectedList.classList.add('selected-list');
                    predictionContainer.appendChild(selectedList);
                    const selectedUl = document.createElement('ul');
                    selectedIngredients.forEach(ingredient => {
                        const selectedListItem = document.createElement('li');
                        selectedListItem.textContent = ingredient;
                        selectedUl.appendChild(selectedListItem);
                    });
                    selectedList.appendChild(selectedUl);
                }
                else{
                    selectedIngredients = uniqueClasses;
                }
                console.log('selected Ingredients = ' + selectedIngredients)